FROM alpine:latest

WORKDIR /app

# Copy the pre-built binary
COPY api-only .

# Copy config file
COPY config.yaml .

# Make binary executable
RUN chmod +x api-only

# Expose port
EXPOSE 8080

# Run the application
CMD ["./api-only"] 